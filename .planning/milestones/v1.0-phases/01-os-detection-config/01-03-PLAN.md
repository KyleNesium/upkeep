---
phase: 01-os-detection-config
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - upkeep/skills/cleanquick/SKILL.md
autonomous: true
requirements: [OS-01, OS-02, OS-03, OS-04, CFG-01]

must_haves:
  truths:
    - "Running /upkeep:cleanquick on Linux prints detected OS/distro at top of Phase 1 without erroring"
    - "Running /upkeep:cleanquick on WSL2 prints 'wsl2' (not 'linux')"
    - "Running /upkeep:cleanquick on Linux/WSL2 skips Phases 2 and 11 with 'skipped (macOS only)' note"
    - "Running /upkeep:cleanquick on macOS behaves exactly as before v1.0.6 — zero regression"
    - "allowed-tools lists uname, lsb_release, lsblk, cat, apt, dnf, pacman, snap, flatpak, systemctl, journalctl"
  artifacts:
    - path: "upkeep/skills/cleanquick/SKILL.md"
      provides: "Quick clean skill with OS detection + macOS guards + Linux allowed-tools"
      contains: "OS_TYPE=\"macos\""
    - path: "upkeep/skills/cleanquick/SKILL.md"
      provides: "Linux commands in allowed-tools frontmatter"
      contains: "- Bash(uname *)"
  key_links:
    - from: "Phase 1 Baseline"
      to: "OS_TYPE variable"
      via: "bash assignment block at top of instructions"
      pattern: "OS_TYPE=\"(macos|linux|wsl2)\""
    - from: "Phase 2 (Homebrew) and Phase 11 (Electron caches)"
      to: "OS_TYPE guard"
      via: "bash conditional at phase entry"
      pattern: "\\[ \"\\$OS_TYPE\" != \"macos\" \\]"
---

<objective>
Update the cleanquick skill (the fast 6-phase cache sweep: 1, 2, 3, 8, 11, 13) so it detects host OS + distro before Phase 1 and guards the two macOS-only phases (Homebrew, Electron caches) with graceful skips. Also extend allowed-tools for Linux commands.

Purpose: cleanquick runs monthly maintenance and is the most-used variant. It has fewer macOS-only hooks than cleandeep (just brew and ~/Library/Application Support paths), so this is a smaller surgical update — but the detection snippet must be identical to other Phase 1 plans so cross-skill behavior is consistent.
Output: Updated upkeep/skills/cleanquick/SKILL.md with detection snippet, 2 phase guards (Phase 2, Phase 11), and extended allowed-tools.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@upkeep/skills/cleanquick/SKILL.md

<canonical_os_detection_snippet>
Identical to Plans 01-01, 01-02. Insert verbatim after the intro sentence ("You are a macOS system cleanup specialist. Run the quick phases only: 1, 2, 3, 8, 11, 13. Report sizes, ask before removing. Never run sudo. Tag each phase header with `(Quick)`.") and BEFORE `## Phase 1: Baseline (Quick)`.

```bash
# ── OS Detection (run once, export for all phases) ────────────────
_KERNEL=$(uname -s 2>/dev/null || echo "unknown")
_KREL=$(uname -r 2>/dev/null || echo "")
case "$_KERNEL" in
  Darwin)
    OS_TYPE="macos"
    OS_DISTRO="macos"
    ;;
  Linux)
    if echo "$_KREL" | grep -qi "microsoft"; then
      OS_TYPE="wsl2"
    else
      OS_TYPE="linux"
    fi
    if [ -r /etc/os-release ]; then
      OS_DISTRO=$(. /etc/os-release 2>/dev/null; echo "${ID_LIKE:-$ID}" | awk '{print $1}')
    elif command -v lsb_release >/dev/null 2>&1; then
      OS_DISTRO=$(lsb_release -si 2>/dev/null | tr '[:upper:]' '[:lower:]')
    else
      OS_DISTRO="unknown"
    fi
    case "$OS_DISTRO" in
      debian|ubuntu) PKG_MGR="apt" ;;
      fedora|rhel|centos|rocky|almalinux) PKG_MGR="dnf" ;;
      arch|manjaro|endeavouros) PKG_MGR="pacman" ;;
      *) PKG_MGR="unknown" ;;
    esac
    ;;
  *)
    OS_TYPE="unknown"
    OS_DISTRO="unknown"
    PKG_MGR="unknown"
    ;;
esac
export OS_TYPE OS_DISTRO PKG_MGR
echo "Environment: $OS_TYPE / $OS_DISTRO${PKG_MGR:+ (pkg: $PKG_MGR)}"
```
</canonical_os_detection_snippet>

<canonical_macos_guard_pattern>
```bash
if [ "$OS_TYPE" != "macos" ]; then
  echo "Phase <N>: skipped (macOS only) — detected $OS_TYPE"
  # Stop this phase here. Continue to the next phase.
fi
```
Prose follow-up: "If the guard prints the skip line, stop this phase and move to the next."
</canonical_macos_guard_pattern>

<macos_only_phases_in_cleanquick>
Only two phases need guards in upkeep/skills/cleanquick/SKILL.md:
- Phase 2: Homebrew Audit (Quick) — brew is macOS-default; Linux Homebrew handling arrives in Phase 2 of roadmap
- Phase 11: Electron App Caches (Quick) — uses ~/Library/Application Support paths

NOT guarded (cross-platform): Phase 1 (Baseline), Phase 3 (Dev Tool Caches), Phase 8 (Project Build Artifacts), Phase 13 (Trash).

Note: Phase 3's `du -sh ~/Library/Caches/*/` line is macOS-specific but the ~/.cache/ line is Linux — both coexist fine (Linux just returns empty for Library/Caches). Phase 2 of roadmap will split properly. For Phase 1 of roadmap, we leave Phase 3 unguarded so the ~/.cache/ scan runs on Linux.
</macos_only_phases_in_cleanquick>

<interfaces>
Current allowed-tools in upkeep/skills/cleanquick/SKILL.md permits: diskutil, df, du, ls, stat, find, sw_vers, echo, date, touch, command, which, pgrep, text tools, brew, npm, yarn, pnpm, bun, pip, pipx, uv, go, cargo, pod, gem, rm, Read, Glob, Grep.

Additions required for CFG-01:
- Under `# Read-only discovery / queries`: `- Bash(uname *)`, `- Bash(lsb_release *)`, `- Bash(lsblk *)`, `- Bash(cat *)`
- New section `# Linux system tools`: `- Bash(systemctl *)`, `- Bash(journalctl *)`
- New section `# Linux package managers`: `- Bash(apt *)`, `- Bash(dnf *)`, `- Bash(pacman *)`, `- Bash(snap *)`, `- Bash(flatpak *)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend cleanquick allowed-tools frontmatter with Linux commands</name>
  <files>upkeep/skills/cleanquick/SKILL.md</files>
  <read_first>
    - upkeep/skills/cleanquick/SKILL.md (read lines 1-55 to see frontmatter structure)
  </read_first>
  <action>
    Edit YAML frontmatter `allowed-tools` in upkeep/skills/cleanquick/SKILL.md. Additions are ADDITIVE only.

    1. Under `# Read-only discovery / queries` (currently ending with `- Bash(pgrep *)` around line 25, before `# Text processing for pipelines`), append:

    ```yaml
      # OS detection (cross-platform)
      - Bash(uname *)
      - Bash(lsb_release *)
      - Bash(lsblk *)
      - Bash(cat *)
    ```

    2. At the END of allowed-tools list (after `- Grep`, before closing `---`), append:

    ```yaml
      # Linux system tools
      - Bash(systemctl *)
      - Bash(journalctl *)
      # Linux package managers
      - Bash(apt *)
      - Bash(dnf *)
      - Bash(pacman *)
      - Bash(snap *)
      - Bash(flatpak *)
    ```

    3. Bump `version: 1.0.6` to `version: 1.1.0-dev` at line 2.
  </action>
  <verify>
    <automated>grep -qE "^\s*- Bash\(uname \*\)$" upkeep/skills/cleanquick/SKILL.md && grep -qE "^\s*- Bash\(lsb_release \*\)$" upkeep/skills/cleanquick/SKILL.md && grep -qE "^\s*- Bash\(lsblk \*\)$" upkeep/skills/cleanquick/SKILL.md && grep -qE "^\s*- Bash\(cat \*\)$" upkeep/skills/cleanquick/SKILL.md && grep -qE "^\s*- Bash\(systemctl \*\)$" upkeep/skills/cleanquick/SKILL.md && grep -qE "^\s*- Bash\(journalctl \*\)$" upkeep/skills/cleanquick/SKILL.md && grep -qE "^\s*- Bash\(apt \*\)$" upkeep/skills/cleanquick/SKILL.md && grep -qE "^\s*- Bash\(dnf \*\)$" upkeep/skills/cleanquick/SKILL.md && grep -qE "^\s*- Bash\(pacman \*\)$" upkeep/skills/cleanquick/SKILL.md && grep -qE "^\s*- Bash\(snap \*\)$" upkeep/skills/cleanquick/SKILL.md && grep -qE "^\s*- Bash\(flatpak \*\)$" upkeep/skills/cleanquick/SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 11 new entries (uname, lsb_release, lsblk, cat, systemctl, journalctl, apt, dnf, pacman, snap, flatpak) present
    - Comments `# OS detection (cross-platform)`, `# Linux system tools`, `# Linux package managers` present
    - Existing entries (`- Bash(diskutil *)`, `- Bash(brew *)`, `- Bash(npm *)`, `- Bash(rm *)`) still present — no deletions
    - `version: 1.1.0-dev` at line 2
    - YAML valid
  </acceptance_criteria>
  <done>cleanquick frontmatter contains all 11 new entries; version bumped; no existing entries removed.</done>
</task>

<task type="auto">
  <name>Task 2: Insert OS detection snippet and guard Phases 2 and 11 in cleanquick</name>
  <files>upkeep/skills/cleanquick/SKILL.md</files>
  <read_first>
    - upkeep/skills/cleanquick/SKILL.md (entire file — focus on intro sentence, `## Phase 1: Baseline (Quick)`, `## Phase 2: Homebrew Audit (Quick)`, `## Phase 11: Electron App Caches (Quick)`)
  </read_first>
  <action>
    Two edits in one task (single file, tightly related).

    **Edit A — OS detection snippet:** Insert after the intro sentence ("You are a macOS system cleanup specialist. Run the quick phases only: 1, 2, 3, 8, 11, 13. Report sizes, ask before removing. Never run sudo. Tag each phase header with `(Quick)`.") and BEFORE `## Phase 1: Baseline (Quick)`:

    ```markdown
    ## Environment Detection

    Run this FIRST, before Phase 1. It sets `$OS_TYPE` (macos / linux / wsl2), `$OS_DISTRO`, and `$PKG_MGR` — Phase 2 and Phase 11 gate on `$OS_TYPE = "macos"`.

    ```bash
    # ── OS Detection (run once, export for all phases) ────────────────
    _KERNEL=$(uname -s 2>/dev/null || echo "unknown")
    _KREL=$(uname -r 2>/dev/null || echo "")
    case "$_KERNEL" in
      Darwin)
        OS_TYPE="macos"
        OS_DISTRO="macos"
        ;;
      Linux)
        if echo "$_KREL" | grep -qi "microsoft"; then
          OS_TYPE="wsl2"
        else
          OS_TYPE="linux"
        fi
        if [ -r /etc/os-release ]; then
          OS_DISTRO=$(. /etc/os-release 2>/dev/null; echo "${ID_LIKE:-$ID}" | awk '{print $1}')
        elif command -v lsb_release >/dev/null 2>&1; then
          OS_DISTRO=$(lsb_release -si 2>/dev/null | tr '[:upper:]' '[:lower:]')
        else
          OS_DISTRO="unknown"
        fi
        case "$OS_DISTRO" in
          debian|ubuntu) PKG_MGR="apt" ;;
          fedora|rhel|centos|rocky|almalinux) PKG_MGR="dnf" ;;
          arch|manjaro|endeavouros) PKG_MGR="pacman" ;;
          *) PKG_MGR="unknown" ;;
        esac
        ;;
      *)
        OS_TYPE="unknown"
        OS_DISTRO="unknown"
        PKG_MGR="unknown"
        ;;
    esac
    export OS_TYPE OS_DISTRO PKG_MGR
    echo "Environment: $OS_TYPE / $OS_DISTRO${PKG_MGR:+ (pkg: $PKG_MGR)}"
    ```

    If `$OS_TYPE` is `unknown`, run Phase 1 (Baseline) only and skip remaining phases.
    ```

    **Edit B — Phase guards:** Add this exact guard block at the TOP of Phase 2 and Phase 11 bodies:

    ```bash
    if [ "$OS_TYPE" != "macos" ]; then
      echo "Phase <N>: skipped (macOS only) — detected $OS_TYPE"
      # Stop this phase here. Continue to the next phase.
    fi
    ```

    After each guard add the prose: "If the guard prints the skip line, stop this phase and move to the next."

    1. **Phase 2: Homebrew Audit (Quick)** — guard BEFORE the existing `command -v brew >/dev/null 2>&1` check. Replace `<N>` with `2`.
    2. **Phase 11: Electron App Caches (Quick)** — guard at top of Phase 11 body (before the `find ~/Library/Application\ Support` command). Replace `<N>` with `11`.

    DO NOT guard Phase 1 (Baseline), Phase 3 (Dev Tool Caches — ~/.cache/ works on Linux), Phase 8 (Project Build Artifacts — cross-platform), Phase 13 (Trash — Linux uses ~/.local/share/Trash but unguarded for now; Phase 2 of roadmap handles it).

    Do NOT modify any other phase content or existing `command -v brew` / `pgrep` checks — they remain intact below the OS guard.
  </action>
  <verify>
    <automated>grep -q "^## Environment Detection$" upkeep/skills/cleanquick/SKILL.md && grep -q '_KERNEL=$(uname -s' upkeep/skills/cleanquick/SKILL.md && grep -q 'OS_TYPE="wsl2"' upkeep/skills/cleanquick/SKILL.md && grep -q 'PKG_MGR="apt"' upkeep/skills/cleanquick/SKILL.md && grep -q 'export OS_TYPE OS_DISTRO PKG_MGR' upkeep/skills/cleanquick/SKILL.md && grep -q 'Phase 2: skipped (macOS only)' upkeep/skills/cleanquick/SKILL.md && grep -q 'Phase 11: skipped (macOS only)' upkeep/skills/cleanquick/SKILL.md && grep -q 'command -v brew' upkeep/skills/cleanquick/SKILL.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `## Environment Detection` heading exactly once, appears BEFORE `## Phase 1: Baseline (Quick)`
    - Detection snippet contains: `uname -s`, `OS_TYPE="macos"`, `OS_TYPE="linux"`, `OS_TYPE="wsl2"`, `microsoft`, `/etc/os-release`, `PKG_MGR="apt"`, `PKG_MGR="dnf"`, `PKG_MGR="pacman"`, `export OS_TYPE OS_DISTRO PKG_MGR`
    - `Phase 2: skipped (macOS only)` present; `Phase 11: skipped (macOS only)` present
    - `grep -c 'OS_TYPE.*!=.*"macos"' upkeep/skills/cleanquick/SKILL.md` returns >= 2
    - Phase 2's OS guard appears BEFORE the existing `command -v brew` check — the `command -v brew` check is NOT removed
    - Existing `pgrep -x` Electron check in Phase 11 still present below the new OS guard
    - No guard added to Phase 1, 3, 8, 13 — spot-check: text immediately after `## Phase 3: Dev Tool Caches (Quick)` is still "**Discovery-based.**"
    - `grep -c "^## Phase " upkeep/skills/cleanquick/SKILL.md` returns 6 (no phase headings added/removed — cleanquick only has 6 phases: 1, 2, 3, 8, 11, 13)
  </acceptance_criteria>
  <done>Environment Detection section inserted; two phase guards (Phase 2, Phase 11) in place; macOS behavior unchanged.</done>
</task>

</tasks>

<verification>
Structural:
- `grep -E "^(---|## )" upkeep/skills/cleanquick/SKILL.md` — one new `## Environment Detection` heading; all 6 `## Phase N:` headings (1, 2, 3, 8, 11, 13) intact
- `head -2 upkeep/skills/cleanquick/SKILL.md` shows `---` then `name: upkeep:cleanquick`

macOS regression:
- With `$OS_TYPE="macos"`, both guards fall through → Phase 2 runs `brew outdated`, Phase 11 runs find on ~/Library/Application Support

Linux dry-read:
- Phase 1 prints `Environment: linux / ...` then Baseline commands
- Phase 2 prints `Phase 2: skipped (macOS only) — detected linux` and halts
- Phase 3 runs (~/.cache/ scan works; ~/Library/Caches is empty — acceptable)
- Phase 8 runs (node_modules, .venv scans work)
- Phase 11 prints `Phase 11: skipped (macOS only)` and halts
- Phase 13 runs (~/.Trash empty on Linux — Phase 2 of roadmap adds ~/.local/share/Trash)
</verification>

<success_criteria>
- Both tasks pass automated verify commands
- cleanquick/SKILL.md has 11 new allowed-tools entries and OS detection snippet
- 2 phase guards (Phase 2, Phase 11)
- macOS behavior unchanged
- Requirements OS-01, OS-02, OS-03, OS-04, CFG-01 satisfied for cleanquick
</success_criteria>

<output>
After completion, create `.planning/phases/01-os-detection-config/01-03-SUMMARY.md`.
</output>
