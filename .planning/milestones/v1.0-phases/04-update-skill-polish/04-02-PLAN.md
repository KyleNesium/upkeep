---
phase: 04-update-skill-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - upkeep/skills/upkeep/SKILL.md
  - README.md
autonomous: true
requirements:
  - CFG-02
  - CFG-03

must_haves:
  truths:
    - "upkeep/skills/upkeep/SKILL.md description frontmatter and intro paragraph state that the skill supports macOS, Linux (Debian/Ubuntu, Fedora/RHEL, Arch), and WSL2"
    - "README.md Prerequisites section lists macOS 14+, Linux (Debian/Ubuntu, Fedora/RHEL, Arch), and WSL2 as supported platforms"
    - "README.md top badge row includes Linux and WSL2 badges alongside the existing macOS badge"
    - "README.md adds a 'Platform Support' section enumerating each platform and its tested package managers"
    - "README.md 'Why upkeep?' subtitle no longer claims macOS-only in conflict with the new support matrix"
    - "Cleanup Categories table is annotated so macOS-only phases are visually distinguishable from cross-platform phases (Phase 2, 4, 5, 6, 11, 14 are macOS-only; 1, 3, 7, 8, 9, 10, 12, 13, 15 are cross-platform)"
  artifacts:
    - path: "upkeep/skills/upkeep/SKILL.md"
      provides: "Cross-platform skill description — frontmatter + intro reflect macOS/Linux/WSL2"
      contains: "Linux"
    - path: "README.md"
      provides: "Prerequisites, badges, and Platform Support section covering all three platforms"
      contains: "WSL2"
  key_links:
    - from: "upkeep/skills/upkeep/SKILL.md description frontmatter"
      to: "router intro paragraph"
      via: "consistent cross-platform language — description and prose must agree"
      pattern: "(macOS|Linux|WSL2)"
    - from: "README.md Prerequisites section"
      to: "README.md Platform Support section"
      via: "both enumerate the same set of platforms in the same order"
      pattern: "WSL2"
    - from: "README.md top-of-file badges"
      to: "README.md Prerequisites"
      via: "visual parity — badges echo what the text claims"
      pattern: "img.shields.io"
---

<objective>
Bring the upkeep router skill description and the project README into alignment with the cross-platform reality established by Phases 1-3. Update description language, add Linux/WSL2 badges, rewrite the Prerequisites section, and add a Platform Support table.

Purpose: Closes CFG-02 and CFG-03. Without this, the SKILL and README still advertise macOS-only while the skills themselves now handle Linux and WSL2.
Output: Modified upkeep/skills/upkeep/SKILL.md (description + intro) and README.md (badges + subtitle + Prerequisites + new Platform Support section + Cleanup Categories annotation).
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-update-skill-polish/04-CONTEXT.md

# Target files
@upkeep/skills/upkeep/SKILL.md
@README.md

<current_state>
upkeep/skills/upkeep/SKILL.md (lines 1-14 — frontmatter description block starting at line 5 `description: |`):
Currently says "macOS system cleanup. Three modes: deep..." — mentions only macOS.
Line 113-115 intro: "You are a macOS system cleanup specialist. Audit the machine..."

README.md key sections to modify:
- Line 7 subtitle: "**macOS system cleanup and updater Skill for Claude Code**"
- Lines 9-12 badges: macOS, Claude Code, Version, License (no Linux or WSL2 badges)
- Lines 52-57 Prerequisites: macOS 14+, Claude Code, Homebrew only
- Lines 130-148 Cleanup Categories table: no platform column
- No Platform Support section exists
</current_state>

<platform_matrix>
Established by Phases 1-3 (all complete):

| Platform      | OS_TYPE | Package managers                  | Notes                                              |
|---------------|---------|-----------------------------------|----------------------------------------------------|
| macOS 14+     | macos   | brew, mas                         | Full 15-phase coverage                             |
| Ubuntu/Debian | linux   | apt, snap (opt), flatpak (opt)    | Linux cleanup phases + Phase 4 upgrade paths       |
| Fedora/RHEL   | linux   | dnf, flatpak (opt)                | Linux cleanup phases + Phase 4 upgrade paths       |
| Arch          | linux   | pacman, flatpak (opt)             | Linux cleanup phases + Phase 4 upgrade paths       |
| WSL2          | wsl2    | Linux pkg mgr + Windows audit-only (winget/scoop/choco) | Linux coverage + banner + /mnt/c bonus phases |

macOS-only phases (guarded by `$OS_TYPE != "macos"` skip):
  Phase 2 Homebrew, Phase 4 Orphaned App Data, Phase 5 LaunchAgents, Phase 6 Xcode, Phase 11 Electron, Phase 14 iOS Backups

Cross-platform phases:
  Phase 1 Baseline, Phase 3 Dev Tool Caches, Phase 7 Docker, Phase 8 Build Artifacts, Phase 9 Stale Logs, Phase 10 Shell Config, Phase 12 Large Files, Phase 13 Trash, Phase 15 pipx
</platform_matrix>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CFG-02 — Rewrite upkeep router SKILL.md description and intro for cross-platform</name>
  <files>upkeep/skills/upkeep/SKILL.md</files>
  <read_first>
    - upkeep/skills/upkeep/SKILL.md (entire frontmatter + first paragraph of body, lines 1-120)
    - .planning/phases/04-update-skill-polish/04-CONTEXT.md
  </read_first>
  <action>
In upkeep/skills/upkeep/SKILL.md, make TWO edits. Do NOT modify allowed-tools, version, author, name, or the Environment Detection bash block (starts at line 122).

**Edit A — frontmatter description block** (lines 5-14, the multi-line YAML after `description: |`). Replace the entire block (preserving the same indentation — two-space indent after `description: |`) with:

```yaml
description: |
  Cross-platform system cleanup and updates for macOS 14+, Linux (Debian/Ubuntu,
  Fedora/RHEL, Arch), and WSL2. Three cleanup modes: deep (full phase audit +
  cleanup), quick (caches + package manager sweep), audit (report only, no
  changes). Plus update mode for AI skills and package managers. Discovery-based
  orphan detection with before/after disk tracking. On macOS: Homebrew, dev caches,
  orphaned app data, LaunchAgents, Xcode, Docker, build artifacts, Electron, shell
  config, logs, large files, iOS backups, pipx tools. On Linux: apt/dnf/pacman
  package cache, ~/.cache sweep, systemd journal vacuum, snap/flatpak cleanup,
  orphaned kernels. On WSL2: everything Linux offers plus Windows temp and
  %LOCALAPPDATA% cache audit via /mnt/c bridge.
  Use when: "clean up my mac", "clean up my linux box", "clean up wsl", "disk cleanup",
  "free up space", "audit my system", "what is taking up space", "new machine setup",
  "mac cleanup", "ubuntu cleanup", "fedora cleanup", "arch cleanup", "wsl2 cleanup".
  Also handles updates: "update upkeep", "update my AI skills", "update everything",
  "check for updates", "upgrade my packages", "update all my tools", "is upkeep up to date".
```

**Edit B — intro paragraph** (currently line 115 — body text below the frontmatter and below the `# /upkeep — macOS System Cleanup` heading). Current text:

```
You are a macOS system cleanup specialist. Audit the machine for reclaimable disk
space, stale data, and configuration issues. Clean up with user approval.
```

Replace with:

```
You are a cross-platform system cleanup specialist supporting macOS 14+, Linux
(Debian/Ubuntu, Fedora/RHEL, Arch), and WSL2. Audit the machine for reclaimable
disk space, stale data, and configuration issues. Clean up with user approval.
Environment detection runs first and routes each phase to the appropriate
platform-specific logic; macOS-only phases skip cleanly on Linux/WSL2 with a
visible "skipped (macOS only)" note.
```

Also update the H1 heading on line 113 from `# /upkeep — macOS System Cleanup` to `# /upkeep — Cross-Platform System Cleanup`.
  </action>
  <verify>
    <automated>grep -n 'Cross-platform system cleanup and updates for macOS 14+, Linux' upkeep/skills/upkeep/SKILL.md && grep -n 'cross-platform system cleanup specialist supporting macOS 14+' upkeep/skills/upkeep/SKILL.md && grep -n '# /upkeep — Cross-Platform System Cleanup' upkeep/skills/upkeep/SKILL.md && ! grep -n 'You are a macOS system cleanup specialist\.' upkeep/skills/upkeep/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - grep `Cross-platform system cleanup and updates for macOS 14\+, Linux` returns 1 match in description frontmatter
    - grep `cross-platform system cleanup specialist supporting macOS 14\+` returns 1 match in body intro
    - grep `Debian/Ubuntu` returns at least 1 match (Linux distros enumerated)
    - grep `WSL2` returns at least 2 matches (description + intro)
    - grep `apt/dnf/pacman` returns at least 1 match (Linux coverage enumerated)
    - grep `/mnt/c` returns at least 1 match (WSL2 coverage enumerated)
    - grep `# /upkeep — Cross-Platform System Cleanup` returns 1 match (H1 updated)
    - grep `# /upkeep — macOS System Cleanup` returns ZERO matches (old H1 removed)
    - grep `You are a macOS system cleanup specialist\.` returns ZERO matches (old intro removed)
    - YAML frontmatter still valid: line 1 is `---`, closing `---` precedes H1, no broken indentation in `description: |` block
    - Unchanged: grep `name: upkeep` returns 1 match, grep `version: 1.1.0-dev` returns 1 match, grep `author: KyleNesium` returns 1 match
    - Unchanged: grep `## Environment Detection` still returns 1 match
    - Unchanged: allowed-tools block still intact — grep `  - Bash(apt \*)` returns 1 match
  </acceptance_criteria>
  <done>upkeep router description, H1, and intro declare cross-platform support across macOS/Linux/WSL2 with distro specifics; rest of file unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: CFG-03 — Update README subtitle, badges, and Prerequisites for cross-platform</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines 1-60 — header, subtitle, badges, Why upkeep, Prerequisites)
    - .planning/phases/04-update-skill-polish/04-CONTEXT.md
  </read_first>
  <action>
In README.md, make THREE edits. Cleanup Categories table and new Platform Support section come in Task 3.

**Edit A — subtitle and tagline** (lines 7-8). Current text:

```
**macOS system cleanup and updater Skill for Claude Code**

Discovery-based disk audit, cleanup, and one-command updates. Finds orphaned app data, stale caches, dead LaunchAgents, and configuration drift. Also updates AI skills (upkeep, gstack, etc.) and package managers (brew, npm, pipx, gems, rustup, bun, deno, mise, uv) in one sweep.
```

Replace with:

```
**Cross-platform system cleanup and updater Skill for Claude Code**

Discovery-based disk audit, cleanup, and one-command updates for macOS 14+, Linux (Debian/Ubuntu, Fedora/RHEL, Arch), and WSL2. Finds orphaned app data, stale caches, dead LaunchAgents, Linux package cruft, systemd journal bloat, and configuration drift. Also updates AI skills (upkeep, gstack, etc.) and package managers (brew, apt/dnf/pacman, snap, flatpak, npm, pipx, gems, rustup, bun, deno, mise, uv) in one sweep.
```

**Edit B — badge row** (lines 9-12). Insert Linux and WSL2 badges between the existing macOS badge and the Claude Code badge. Preserve the existing macOS, Claude Code, Version, and License badges byte-for-byte (do NOT regenerate their URLs — read them from README.md and reuse). The final badge block:

```markdown
[![macOS](https://img.shields.io/badge/macOS-14%2B-000?logo=apple&logoColor=white)](https://www.apple.com/macos/)
[![Linux](https://img.shields.io/badge/Linux-Debian%20%7C%20Fedora%20%7C%20Arch-FCC624?logo=linux&logoColor=black)](https://www.kernel.org/)
[![WSL2](https://img.shields.io/badge/WSL2-supported-4EAA25?logo=windowsterminal&logoColor=white)](https://learn.microsoft.com/windows/wsl/)
<existing Claude Code badge line — preserve verbatim>
<existing Version badge line — preserve verbatim>
<existing License badge line — preserve verbatim>
```

The Claude Code badge in the current README uses a specific base64-encoded inline SVG logo — do not hand-type it, copy it from the file. Similarly the Version badge URL must be preserved as-is.

**Edit C — Prerequisites section** (lines 52-57). Replace the existing three-bullet Prerequisites section with:

```
## Prerequisites

### Supported platforms

- **macOS 14+** (Sonoma or later) — full 15-phase coverage
- **Linux** — Debian/Ubuntu (apt), Fedora/RHEL (dnf), Arch (pacman). Optional: snap, flatpak.
- **WSL2** on Windows 10/11 — Ubuntu, Debian, Fedora, or Arch distro. /mnt/c bridge required for Windows-side bonus phases.

### Required

- **[Claude Code](https://docs.anthropic.com/en/docs/claude-code)** (for `/upkeep` slash command)

### Optional (detected at runtime — skipped gracefully if absent)

- **macOS:** Homebrew, Xcode Command Line Tools, Docker
- **Linux/WSL2:** apt / dnf / pacman, snap, flatpak, systemd (for journalctl vacuum)
- **Both:** git (for skill updates), node/npm/bun/pipx/uv/cargo/etc. for their respective cache cleanups
```

Do NOT touch the `## Why upkeep?` section, `## Install`, Table of Contents, or anything else in this task.
  </action>
  <verify>
    <automated>grep -n '\*\*Cross-platform system cleanup and updater Skill' README.md && grep -n 'Linux-Debian%20%7C%20Fedora%20%7C%20Arch' README.md && grep -n 'WSL2-supported' README.md && grep -n '### Supported platforms' README.md && grep -n 'Debian/Ubuntu (apt), Fedora/RHEL (dnf), Arch (pacman)' README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep `**Cross-platform system cleanup and updater Skill for Claude Code**` returns 1 match (subtitle updated)
    - grep `**macOS system cleanup and updater Skill for Claude Code**` returns ZERO matches (old subtitle removed)
    - grep `img.shields.io/badge/Linux-Debian` returns 1 match (Linux badge added)
    - grep `img.shields.io/badge/WSL2-supported` returns 1 match (WSL2 badge added)
    - grep `img.shields.io/badge/macOS-14%2B` returns 1 match (existing macOS badge preserved)
    - grep `### Supported platforms` returns 1 match (new Prerequisites subsection)
    - grep `### Required` returns at least 1 match
    - grep `### Optional (detected at runtime` returns 1 match
    - grep `Debian/Ubuntu (apt), Fedora/RHEL (dnf), Arch (pacman)` returns 1 match (distros enumerated in Prereqs)
    - grep `WSL2` returns at least 3 matches in README.md (badge + Prereqs header + Prereqs bullet)
    - Existing Version badge URL preserved — grep `img.shields.io/github/v/release/KyleNesium` returns 1 match
    - Existing License badge preserved — grep `License: MIT` returns at least 1 match
    - Claude Code badge preserved — grep `Claude_Code-skill-7C3AED` returns 1 match
  </acceptance_criteria>
  <done>README.md header, badge row, and Prerequisites reflect macOS/Linux/WSL2 support. Identity (Claude Code, Version, License) badges preserved byte-for-byte.</done>
</task>

<task type="auto">
  <name>Task 3: CFG-03 — Add Platform Support section and annotate Cleanup Categories table</name>
  <files>README.md</files>
  <read_first>
    - README.md (after Task 2 edits — lines 40-200 covering Why upkeep, Install, Usage, How It Works, Cleanup Categories)
    - .planning/phases/04-update-skill-polish/04-CONTEXT.md (platform_matrix in context)
  </read_first>
  <action>
In README.md, make TWO edits.

**Edit A — Insert "Platform Support" section** immediately AFTER the Prerequisites section (rebuilt by Task 2) and BEFORE the `## Install` heading. Insert this content verbatim (including the `---` separator at the end, matching the separator pattern used elsewhere in the README):

```markdown
## Platform Support

upkeep routes each phase based on detected OS. Phases skip cleanly on platforms where they don't apply (e.g., Homebrew on Linux, systemd journal on macOS) with a visible "skipped" note — never errors.

| Platform       | Package managers                                                  | Platform-specific phases                                                                     |
|----------------|-------------------------------------------------------------------|----------------------------------------------------------------------------------------------|
| macOS 14+      | brew, mas                                                         | Homebrew, LaunchAgents, Xcode, iOS backups, Electron caches, orphaned app data (mdfind)      |
| Ubuntu/Debian  | apt, snap (opt), flatpak (opt)                                    | apt clean, ~/.cache sweep, journalctl vacuum, snap/flatpak cleanup, orphaned kernels + .deb  |
| Fedora/RHEL    | dnf, flatpak (opt)                                                | dnf clean, ~/.cache sweep, journalctl vacuum, flatpak cleanup, orphaned .rpm + kernels       |
| Arch           | pacman, flatpak (opt)                                             | pacman -Sc, ~/.cache sweep, journalctl vacuum, flatpak cleanup, orphaned packages            |
| WSL2 (Windows) | Linux pkg mgr + Windows audit (winget, scoop, choco — audit only) | Everything Linux offers + Windows %TEMP% and %LOCALAPPDATA% npm/pip cache audit via /mnt/c   |

Detection runs once at the top of every skill invocation via `uname -s`, `/etc/os-release`, and `uname -r | grep -qi microsoft` (for WSL2). The result is exported as `$OS_TYPE`, `$OS_DISTRO`, and `$PKG_MGR` for every downstream phase.

Cross-platform phases that run everywhere: disk baseline (Phase 1), dev tool caches (Phase 3), Docker (Phase 7), project build artifacts (Phase 8), stale logs (Phase 9), shell config audit (Phase 10), large files (Phase 12), trash (Phase 13), pipx tools (Phase 15).

---
```

**Edit B — Annotate Cleanup Categories table with a Platform column**. The current table header (around line 132):

```
| # | Category | Deep | Quick | Audit | What it finds | Typical savings |
|---|----------|:----:|:-----:|:-----:|---------------|----------------|
```

Insert a new "Platform" column between "Category" and "Deep":

```
| # | Category | Platform | Deep | Quick | Audit | What it finds | Typical savings |
|---|----------|----------|:----:|:-----:|:-----:|---------------|----------------|
```

Update each of the 15 data rows to add the Platform cell in the correct position. Platform values by phase:

| Phase | Category            | Platform |
|-------|---------------------|----------|
| 1     | Baseline            | all      |
| 2     | Homebrew            | macOS    |
| 3     | Dev caches          | all      |
| 4     | Orphaned app data   | macOS    |
| 5     | LaunchAgents        | macOS    |
| 6     | Xcode & dev tools   | macOS    |
| 7     | Docker              | all      |
| 8     | Build artifacts     | all      |
| 9     | Stale logs          | all      |
| 10    | Shell config        | all      |
| 11    | Electron caches     | macOS    |
| 12    | Large files         | all      |
| 13    | Trash               | all      |
| 14    | iOS backups         | macOS    |
| 15    | pipx tools          | all      |

Do not modify the existing cell contents in Deep/Quick/Audit, "What it finds", or "Typical savings" — only insert the Platform cell into each row.

Add this explanatory paragraph immediately AFTER the updated Cleanup Categories table (before the `---` separator that follows it, if present, or between the table and the next section):

```
Platform column: `all` = runs on macOS/Linux/WSL2. `macOS` = skipped with a "skipped (macOS only)" note on Linux/WSL2. Linux adds its own phases (package cache, ~/.cache sweep, journalctl, snap/flatpak, orphaned kernels) documented in the [Platform Support](#platform-support) section. WSL2 adds Windows %TEMP% and %LOCALAPPDATA% bonus phases on top of Linux coverage.
```
  </action>
  <verify>
    <automated>grep -n '^## Platform Support' README.md && grep -n 'Package managers' README.md && grep -n 'macOS 14\+' README.md && grep -n 'WSL2 (Windows)' README.md && grep -n 'Category | Platform | Deep' README.md && grep -n 'Platform column: .all. = runs on macOS/Linux/WSL2' README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep `^## Platform Support` returns 1 match (new section present)
    - grep `Package managers` returns at least 1 match inside the Platform Support table
    - grep `WSL2 (Windows)` returns 1 match (WSL2 row in Platform Support table)
    - grep `uname -r | grep -qi microsoft` returns 1 match (detection prose)
    - grep `\$OS_TYPE, \$OS_DISTRO, and \$PKG_MGR` returns 1 match
    - grep `Category | Platform | Deep` returns 1 match (Cleanup Categories header updated)
    - grep `Platform column:` returns 1 match (explanatory paragraph added)
    - grep `\[Platform Support\](#platform-support)` returns at least 1 match (internal link)
    - Each of the 15 rows in the Cleanup Categories table now has exactly 8 pipe-delimited cells (not 7). Verify: a grep like `\|\s*1\s*\|\s*Baseline\s*\|\s*all\s*\|` returns 1 match; `\|\s*2\s*\|\s*Homebrew\s*\|\s*macOS\s*\|` returns 1 match.
    - macOS-only phases correctly tagged: grep `\| 2 \| Homebrew \| macOS \|`, `\| 4 \| Orphaned app data \| macOS \|`, `\| 5 \| LaunchAgents \| macOS \|`, `\| 6 \| Xcode & dev tools \| macOS \|`, `\| 11 \| Electron caches \| macOS \|`, `\| 14 \| iOS backups \| macOS \|` — each returns 1 match
    - Cross-platform phases correctly tagged `all`: verify Phase 1, 3, 7, 8, 9, 10, 12, 13, 15 rows contain `| all |` in the Platform column position
    - Platform Support section appears BEFORE `## Install` — verify line number of `## Platform Support` < line number of `## Install`
  </acceptance_criteria>
  <done>README.md has a new Platform Support section with per-platform package manager table; Cleanup Categories table has a Platform column annotating each of the 15 phases as `all` or `macOS`.</done>
</task>

</tasks>

<verification>
Final pass across both modified files:

1. `grep -c 'WSL2' upkeep/skills/upkeep/SKILL.md` — should return at least 2 (description + intro).
2. `grep -c 'WSL2' README.md` — should return at least 6 (badge, Prereqs header, Prereqs bullet, Platform Support row, Cleanup Categories note, plus the existing Update Mode references).
3. `grep -c 'Debian/Ubuntu' upkeep/skills/upkeep/SKILL.md` — at least 1.
4. `grep -c 'Debian/Ubuntu' README.md` — at least 2 (subtitle + Prereqs + Platform Support).
5. `grep -c '^## Platform Support' README.md` — exactly 1.
6. `grep -c '^## Prerequisites' README.md` — exactly 1.
7. `grep -c '### Supported platforms' README.md` — exactly 1.
8. Integrity: `head -1 upkeep/skills/upkeep/SKILL.md` returns `---`; `head -1 README.md` returns `<div align="center">`.
9. No regression: `grep 'name: upkeep' upkeep/skills/upkeep/SKILL.md` returns 1 match; `grep 'License: MIT' README.md` returns at least 1 match.
10. The Cleanup Categories table has 15 data rows (one per phase) and each row has exactly 8 pipe-separated cells after the update.
</verification>

<success_criteria>
- CFG-02: upkeep/skills/upkeep/SKILL.md description frontmatter and intro both mention macOS, Linux, and WSL2 explicitly with distro specifics (Debian/Ubuntu, Fedora/RHEL, Arch)
- CFG-03: README.md shows macOS 14+, Linux, and WSL2 prerequisites with badges for all three platforms; adds a Platform Support section; annotates the Cleanup Categories table with per-phase platform applicability
- Zero regression: allowed-tools block, version, name, author in SKILL.md unchanged; Claude Code + Version + License badges in README preserved byte-for-byte; Privacy, Safety, Security, Contributing, License sections unchanged
- Documentation is internally consistent: subtitle, badges, Prerequisites, and Platform Support all enumerate the same set of supported platforms in the same order
</success_criteria>

<output>
After completion, create `.planning/phases/04-update-skill-polish/04-02-SUMMARY.md` describing:
- Exact lines modified in upkeep/skills/upkeep/SKILL.md (frontmatter description block, H1, intro paragraph)
- Exact lines modified in README.md (subtitle, badge row, Prerequisites section, Cleanup Categories table, new Platform Support section)
- Confirmation that the grep checks in <verification> all pass
- Any deviations from the planned text (e.g., alternate badge URL, different link target for WSL2 badge)
</output>
