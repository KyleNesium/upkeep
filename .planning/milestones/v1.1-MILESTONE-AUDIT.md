---
milestone: v1.1
milestone_name: Update Skill Overhaul (macOS-only)
audited: 2026-05-07T13:50:00Z
audited_by: gsd-autonomous (re-audit after gap closure PR #9 merged)
status: passed
scores:
  requirements: 19/19 wired
  phases: 5/5 passed
  integration: 8/8 wired
  flows: 4/4 traced
gaps: {}
tech_debt: []
runtime_only_claims:
  - "R1: wall time ≤ 50% of v1.0 sequential discovery — requires populated machine"
  - "R8: --user-install auto-injection actual behaviour — requires system Ruby 2.x machine"
  - "N4: partial-failure resilience in practice — requires synthetic injection"
  - "G4 runtime: schema-mismatch refusal — requires forced bad payload"
  - "G1 runtime: ETA self-tuning across runs — requires two consecutive runs"
nyquist:
  enabled: not_configured
  note: "No workflow.nyquist_validation key present; skipping."
---

# v1.1 Re-Audit — Update Skill Overhaul (macOS-only)

## Verdict

**passed** — All 19 requirements (14 functional, 5 non-functional) are
fully wired in source. All 5 audit gaps from the 2026-05-07 12:15 audit
(G1–G5) are closed in `main` via merged PRs #8 and #9. Cross-phase
contracts traced clean end-to-end. Five runtime claims remain queued for
populated-machine verification but do not block milestone completion.

## What changed since the prior audit

| Audit gap (prior `gaps_found` audit) | Closed by | Evidence on main |
|---|---|---|
| G1 — R10 history reader unwired | Phase 10 / PR #9 | `SKILL.md:357–363` — bash reads `$HIST_FILE` into `$HISTORY_JSON`; `SKILL.md:424` — prompt fence interpolates `${HISTORY_JSON}` |
| G2 — R7 `$UPGRADED_*` undefined | Phase 11 / PR #9 | `SKILL.md:509–511` — `mktemp` + `trap`; `SKILL.md:528–536` — per-tool wrapper appends; `SKILL.md:549–550` — `sort -u` populates env vars before Step 4m |
| G3 — R5 `tool_specs.macos.restart_required` unspecified | Phase 10 / PR #9 | `SKILL.md:398–402` — Hard Rule #8 instructs synthesizer to populate the field; `08-CONTEXT.md:110–115` — schema example shows `macos` shape |
| G4 — schema_version refusal not implemented | Phase 11 / PR #9 | `SKILL.md:444–457` — `### Schema gate (check first)` runs `jq -e '.schema_version == "1"'` before the audit short-circuit and approval gate |
| G5 — severity fields unused | Phase 10 / PR #9 | `SKILL.md:377–381` — Hard Rule #1 reads `severity_on_major` / `severity_on_minor` and sorts `plan.warnings[]` high → medium → low |

## Phase verifications (read from disk)

| Phase | Status | Verifier |
|---|---|---|
| 7 — Parallel Discovery Agents | passed | autonomous-workflow |
| 8 — Compatibility Synthesizer | passed | autonomous-workflow |
| 9 — Single-Gate Apply + Post-Flight | passed | autonomous-workflow |
| 10 — Synthesizer Prompt & Context Fixes | passed | gap-closure-execution (closes G1, G3, G5) |
| 11 — Apply Orchestrator Wiring | passed | gap-closure-execution (closes G2, G4) |

## Requirements coverage

| REQ | Status | Phase | Notes |
|---|---|---|---|
| R1 parallel discovery | wired | 7 | Runtime-only acceptance (wall time) |
| R2 structured JSON | wired | 7 | |
| R3 cross-manager risk flags | wired | 8 | 9 edges in compatibility.json |
| R4 major-bump callout | wired | 8 | Hard Rule #2 |
| R5 single approval gate | wired | 9 + 10 | macOS restart leg now has Hard Rule #8 to populate `tool_specs.macos.restart_required` |
| R6 parallel apply | wired | 9 | 4-job cap |
| R7 PATH shadowing | wired | 9 + 11 | `$UPGRADED_*` populated; Step 4m loops have data |
| R8 system Ruby `--user-install` | wired | 8 | Runtime to confirm on Ruby 2.x machine |
| R9 post-flight health check | wired | 9 | brew doctor + command-v + deprecation aggregator |
| R10 ETA in plan | wired | 8 + 10 | Pre-call bash now populates `$HISTORY_JSON`; Hard Rule #6 load-bearing on second run |
| R11 disk-space pre-flight | wired | 9 | Lines 178–186 |
| R12 codex skills git-pull | wired | 7 | Skills-scout cover #2 |
| R13 sub-modes preserved | wired | 9 | Audit short-circuit Step 3m |
| R14 Linux/WSL2 untouched | wired | 7–9 | Routing block + Step-1 reminder + Steps 1–6 preserved |
| N1 no new deps | wired | — | jq optional |
| N2 single SKILL.md + sibling JSON | wired | — | |
| N3 self-contained agent prompts | wired | — | |
| N4 failure isolation | wired | 9 | Per-tool wrapper |
| N5 destructive commands gated | wired | — | macOS restart re-prompt preserved |

19/19 wired. 0 unsatisfied. 0 partial. 0 orphans.

## Cross-phase data flow

| Producer → Consumer | Status |
|---|---|
| Scout 1–4 → combined discovery JSON | wired |
| Combined JSON → synthesizer (Step 2m) | wired (history block now populated) |
| `compatibility.json` → synthesizer | wired (severity fields now consumed) |
| Synthesizer plan → apply orchestrator (Step 3m) | wired (schema gate enforces contract) |
| Synthesizer plan → restart gate | wired (`tool_specs.macos.restart_required` populated by Hard Rule #8) |
| Apply orchestrator → Step 4m post-flight | wired (`$UPGRADED_FORMULAS` / `$UPGRADED_TOOLS` populated) |
| Step 5m history write → next-run Step 2m read | wired (read side now active) |
| macOS routing → Linux/WSL2 routing | wired (Routing block + Step-1 reminder) |

8/8 contracts traced clean.

## E2E flows

| Flow | Status |
|---|---|
| macOS happy path (`/upkeep:update all`) | traced clean |
| macOS audit-mode short-circuit | traced clean (schema gate + audit gate both fire before approval gate) |
| Linux/WSL2 fallback to v1.0 sequential | traced clean (preserved Steps 1–6) |
| Failure isolation (one tool fails, others continue) | traced clean (per-tool wrapper at line 521) |

4/4 flows traced.

## Tech debt

None outstanding. The two prior tech-debt items (G4 schema-version refusal,
G5 severity fields unused) are both closed.

## Runtime-only verification queue

These claims remain unverified and require a populated macOS box. They do
not block milestone completion but should run before declaring v1.1
publicly shipped:

- **R1** — discovery wall time ≤ 50% of v1.0 sequential
- **R8** — `gem update --user-install` auto-injection actual behaviour on a system-Ruby 2.x machine
- **N4** — partial-failure resilience under synthetic mid-run brew failure
- **G1 runtime** — ETA self-tuning measurable difference on second run
- **G3 runtime** — restart gate fires when a real macOS update is pending
- **G4 runtime** — schema-mismatch payload routes to fallback as designed

## Routing

`status: passed`. All requirements satisfied; no blockers; no tech debt.
Proceed to `/gsd:complete-milestone v1.1`.
