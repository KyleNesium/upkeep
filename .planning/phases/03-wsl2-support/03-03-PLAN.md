---
phase: 03-wsl2-support
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - upkeep/skills/update/SKILL.md
autonomous: true
requirements: [WSL-01, WSL-04]
must_haves:
  truths:
    - "Running update inside WSL2 prints 'Running in WSL2 on Windows' banner before any step"
    - "In WSL2, update Step 2 detects Windows package managers — winget, scoop, choco — and reports installed-package counts where accessible"
    - "Windows package managers detected are shown in Step 3 Overview Table under a new 'Windows Packages' section, labeled 'audit only'"
    - "update NEVER runs winget upgrade, scoop update, or choco upgrade in this phase — audit only, no mutation"
    - "If /mnt/c/ is not mounted, the WSL2 Windows package manager detection block prints a skip line and moves on — does not error"
    - "On macOS and plain Linux the Windows package manager block is not executed and nothing about winget/scoop/choco appears in the Overview Table"
  artifacts:
    - path: "upkeep/skills/update/SKILL.md"
      provides: "WSL2 banner in Environment Detection section"
      contains: "Running in WSL2 on Windows"
    - path: "upkeep/skills/update/SKILL.md"
      provides: "Windows package manager detection block in Step 2"
      contains: "Windows package managers (WSL2 only"
    - path: "upkeep/skills/update/SKILL.md"
      provides: "Windows Packages row group in Step 3 overview table template"
      contains: "Windows Packages"
  key_links:
    - from: "Step 2 Discover Packages"
      to: "command -v winget / scoop / choco"
      via: "$OS_TYPE = wsl2 guard + /mnt/c/ accessibility check"
      pattern: "command -v winget"
    - from: "Windows package manager detection"
      to: "Step 3 Overview Table"
      via: "Windows Packages row group"
      pattern: "Windows Packages"
    - from: "Step 5 Apply Package Updates"
      to: "NO winget/scoop/choco execution"
      via: "explicit audit-only note"
      pattern: "audit-only"
---

<objective>
Add two WSL2 additions to `update/SKILL.md`: (1) the WSL-01 banner in Environment Detection, and (2) a WSL-04 Windows package manager detection block in Step 2 that checks for `winget`, `scoop`, and `choco` via `command -v`, reports installed-package counts where available, and surfaces them in the Step 3 Overview Table under a new "Windows Packages" section. This is strictly audit-only — no upgrade commands are added. The existing Step 5 Apply Package Updates table is NOT modified (winget/scoop/choco upgrades are deferred to a later milestone).

Purpose: Satisfies WSL-01 for update and closes WSL-04. WSL2 users often have Windows package managers accessible through the WSL2 PATH (when the Windows PATH is imported). Today `update` silently ignores them; this plan makes their presence visible as an informational row, telling the user "these tools exist but you must upgrade them from a Windows shell" — consistent with the skill's "never run sudo, never auto-upgrade without confirmation" safety posture.

Output: `update/SKILL.md` with the banner block added, allowed-tools extended with winget/scoop/choco query entries, a new Windows package manager detection block appended to Step 2, a new "Windows Packages" group in the Step 3 Overview Table template, and an audit-only note added before the Step 5 upgrade table.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wsl2-support/03-CONTEXT.md

<interfaces>
Current update/SKILL.md structure (as of end of Phase 2):
- Environment Detection (Phase 1 added OS detection; Phase 2 added mas/softwareupdate Linux/WSL2 skip)
- Step 1: Discover AI Skills
- Step 2: Discover Packages (currently ends with mas/softwareupdate else branch inside a single bash block)
- Step 3: Overview Table (currently ends with "Informational" section)
- Step 4: Apply Skill Updates
- Step 5: Apply Package Updates (with Linux/WSL2 mas/macOS skip note)
- Step 6: Final Report
- Rules

Allowed-tools currently has NO winget, NO scoop, NO choco. Since all three run as Windows .exe binaries from WSL2, they are invoked via their names on PATH. For audit-only `command -v <tool>` and `<tool> list`, we need to add:
  - Bash(winget *)
  - Bash(scoop *)
  - Bash(choco *)

Exact tail of Environment Detection block (line ~124):
`echo "Environment: $OS_TYPE / $OS_DISTRO${PKG_MGR:+ (pkg: $PKG_MGR)}"` followed by closing triple-backtick.

Step 2 currently ends with (inside the Step 2 bash block):
```
if [ "$OS_TYPE" = "macos" ]; then
  mas outdated 2>/dev/null
  softwareupdate -l 2>/dev/null | grep -E "^\s*\*"
else
  echo "mas + softwareupdate: skipped (macOS only)"
fi
```
followed by the closing triple-backtick of the Step 2 bash block, then the `## Step 3: Overview Table` header.

Step 3 Overview Table template currently ends with:
```
── Informational ──────────────────────────
  Claude plugins  N (Claude Code manages)
  Codex skills    N (manual update)
```

The `allowed-tools` frontmatter block is at lines 14-63 of update/SKILL.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WSL2 banner + winget/scoop/choco to allowed-tools in update</name>
  <files>upkeep/skills/update/SKILL.md</files>
  <read_first>
    - upkeep/skills/update/SKILL.md (lines 1-63 — frontmatter including allowed-tools)
    - upkeep/skills/update/SKILL.md (lines 84-128 — Environment Detection section)
  </read_first>
  <action>
    Open `upkeep/skills/update/SKILL.md`. Do two additive edits:

    Edit 1 — allowed-tools frontmatter. Find the `# Linux package managers (Phase 4 of roadmap adds upgrade commands)` comment block. Immediately AFTER the `- Bash(flatpak *)` line and BEFORE the closing `---` of the frontmatter, add this block:

    ```
      # Windows package managers (WSL2 audit only — never invoke upgrade commands)
      - Bash(winget *)
      - Bash(scoop *)
      - Bash(choco *)
    ```

    Edit 2 — banner in Environment Detection. Locate the closing triple-backtick of the Environment Detection bash block (the line after `echo "Environment: $OS_TYPE / $OS_DISTRO${PKG_MGR:+ (pkg: $PKG_MGR)}"`). Immediately after that closing fence and before the paragraph starting `If $OS_TYPE is unknown`, insert a blank line followed by a bash fence containing exactly:

    ```
    # ── WSL2 banner (fires only on wsl2) ─────────────────────────────
    if [ "$OS_TYPE" = "wsl2" ]; then
      echo "=== Running in WSL2 on Windows ==="
    fi
    ```

    Do NOT touch the rest of the file in this task.
  </action>
  <verify>
    <automated>grep -q "Running in WSL2 on Windows" upkeep/skills/update/SKILL.md &amp;&amp; grep -q "Bash(winget \*)" upkeep/skills/update/SKILL.md &amp;&amp; grep -q "Bash(scoop \*)" upkeep/skills/update/SKILL.md &amp;&amp; grep -q "Bash(choco \*)" upkeep/skills/update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Running in WSL2 on Windows" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "=== Running in WSL2 on Windows ===" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "Bash(winget \*)" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "Bash(scoop \*)" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "Bash(choco \*)" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "Windows package managers (WSL2 audit only" upkeep/skills/update/SKILL.md` returns exactly `1`
    - The three new allowed-tools entries sit after `Bash(flatpak *)` and before the closing frontmatter `---`
    - Banner block sits immediately after the Environment Detection bash block
  </acceptance_criteria>
  <done>Frontmatter permits winget/scoop/choco query commands and the banner fires in WSL2.</done>
</task>

<task type="auto">
  <name>Task 2: Add Windows package manager detection block to Step 2</name>
  <files>upkeep/skills/update/SKILL.md</files>
  <read_first>
    - upkeep/skills/update/SKILL.md (Step 2 section — ends at `fi` inside the single Step 2 bash block, followed by the closing triple-backtick, then the `## Step 3` header)
  </read_first>
  <action>
    Open `upkeep/skills/update/SKILL.md`. Find the end of Step 2 Discover Packages — the closing triple-backtick of the Step 2 bash block (which comes right after the `fi` of the mas/softwareupdate if/else). Immediately AFTER that closing fence and BEFORE the `## Step 3: Overview Table` header, insert this markdown block (preserve blank lines around it):

    New heading: `### Windows package managers (WSL2 only — audit only)`

    Then a bash fence containing:

    ```
    if [ "$OS_TYPE" = "wsl2" ]; then
      if [ ! -d "/mnt/c" ]; then
        echo "Windows package managers: /mnt/c not mounted — skipping."
      else
        echo "=== Windows package managers (audit — no upgrades run) ==="
        if command -v winget >/dev/null 2>&1; then
          echo "--- winget ---"
          winget list 2>/dev/null | head -5 || echo "(winget accessible but list failed)"
        else
          echo "winget: not on PATH"
        fi
        if command -v scoop >/dev/null 2>&1; then
          echo "--- scoop ---"
          scoop list 2>/dev/null | head -5 || echo "(scoop accessible but list failed)"
        else
          echo "scoop: not on PATH"
        fi
        if command -v choco >/dev/null 2>&1; then
          echo "--- choco ---"
          choco list 2>/dev/null | head -5 || echo "(choco accessible but list failed)"
        else
          echo "choco: not on PATH"
        fi
      fi
    fi
    ```

    Then a quoted paragraph (blockquote) noting audit-only posture:

    > Audit only. This block NEVER runs `winget upgrade`, `scoop update`, or `choco upgrade`. Those require a Windows shell (PowerShell or CMD) — running them from WSL2 has permission and UAC implications that `update` intentionally avoids. When one or more Windows package managers are detected, display this exact guidance after the Windows package manager output:
    >
    > "To upgrade these, open a Windows PowerShell (as administrator if needed) and run `winget upgrade --all`, `scoop update *`, or `choco upgrade all -y` respectively."

    Do NOT modify Step 3, Step 4, Step 5, or Step 6 in this task. Do NOT add any winget/scoop/choco upgrade commands to any bash fence.
  </action>
  <verify>
    <automated>grep -q "### Windows package managers (WSL2 only — audit only)" upkeep/skills/update/SKILL.md &amp;&amp; grep -q "command -v winget" upkeep/skills/update/SKILL.md &amp;&amp; grep -q "command -v scoop" upkeep/skills/update/SKILL.md &amp;&amp; grep -q "command -v choco" upkeep/skills/update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^### Windows package managers (WSL2 only — audit only)$" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "command -v winget" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "command -v scoop" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "command -v choco" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "winget list" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "scoop list" upkeep/skills/update/SKILL.md` returns exactly `1`
    - `grep -c "choco list" upkeep/skills/update/SKILL.md` returns exactly `1`
    - Mentions of `winget upgrade`, `scoop update`, `choco upgrade` appear ONLY inside the informational guidance blockquote — never inside a bash fence that the skill would execute
    - The Windows block is placed between Step 2's closing and the `## Step 3` header: `awk '/^## Step 2/{s2=NR} /^### Windows package managers/{wpm=NR} /^## Step 3/{s3=NR} END{print (s2 < wpm && wpm < s3) ? "OK" : "BAD"}' upkeep/skills/update/SKILL.md` prints `OK`
    - The /mnt/c/ accessibility guard is present: `grep -c 'if \[ ! -d "/mnt/c" \]' upkeep/skills/update/SKILL.md` returns at least `1`
  </acceptance_criteria>
  <done>Step 2 audits Windows package managers on WSL2 with /mnt/c/ guard and per-tool command -v checks, no upgrade commands.</done>
</task>

<task type="auto">
  <name>Task 3: Extend Step 3 Overview Table + Step 5 audit-only note + Step 6 final report row group</name>
  <files>upkeep/skills/update/SKILL.md</files>
  <read_first>
    - upkeep/skills/update/SKILL.md (Step 3 Overview Table section)
    - upkeep/skills/update/SKILL.md (Step 5 Apply Package Updates — paragraph starting `On Linux or WSL2, skip the mas and macOS rows below`)
    - upkeep/skills/update/SKILL.md (Step 6 Final Report template)
  </read_first>
  <action>
    Open `upkeep/skills/update/SKILL.md`. Three edits:

    Edit 1 — Step 3 Overview Table template. Find the Overview Table code fence. It currently contains an `── Informational ──` section with `Claude plugins` and `Codex skills` rows. Immediately AFTER the `Codex skills    N (manual update)` line and BEFORE the closing triple-backtick of the Overview Table block, insert:

    ```
    ── Windows Packages (WSL2 only — audit only) ──
      winget       N installed  (upgrade via Windows PowerShell)
      scoop        N installed  (upgrade via Windows PowerShell)
      choco        N installed  (upgrade via Windows PowerShell)
    ```

    Then, immediately after the closing triple-backtick of the Overview Table, find the existing paragraph that begins `Omit any row where the tool is not installed.` and append a NEW paragraph AFTER it (keep existing paragraph intact):

    ```
    On macOS or plain Linux, omit the entire "Windows Packages" group — it appears only when $OS_TYPE = "wsl2". For each Windows tool not found via command -v, omit that row. Windows package managers are labeled "audit only" because update never invokes winget upgrade, scoop update, or choco upgrade — surface guidance for the user to run those from a Windows shell instead.
    ```

    Edit 2 — Step 5 explicit audit-only note. Find the paragraph in Step 5 that begins `On Linux or WSL2, skip the \`mas\` and \`macOS\` rows below`. Immediately AFTER that paragraph and BEFORE the `| Tool | Audit command | Apply command | Extra warning |` table header, insert a new paragraph:

    ```
    On WSL2, the Step 2 "Windows package managers" block is audit-only — this Step 5 table does NOT include winget, scoop, or choco. Upgrades for those require a Windows PowerShell session and are intentionally out of scope for update. The Step 6 final report lists each detected Windows package manager under "Windows Packages" with status "audit only" so the skip is visible rather than silent.
    ```

    Edit 3 — Step 6 final report template. Find the Step 6 report code fence. It currently ends with an `── Informational ──` section (`Claude plugins  9  (managed by Claude Code)` and `Codex skills   12  (manual update required)`). Immediately AFTER the `Codex skills   12  (manual update required)` line and BEFORE the closing triple-backtick, insert:

    ```
    ── Windows Packages (WSL2 only) ─────────────────
      winget   ⓘ audit only    N installed
      scoop    ⓘ audit only    N installed
      choco    ⓘ audit only    N installed
    ```

    Then, immediately after the closing triple-backtick of the Step 6 final report, find the existing paragraph beginning `Omit rows for tools not installed on this machine.` and append a new sentence:

    ```
    Omit the entire "Windows Packages" group on macOS or plain Linux. In WSL2, omit any row whose tool was not detected by command -v in Step 2.
    ```

    Do NOT add rows for winget/scoop/choco to the Step 5 `| Tool | Audit command | ... |` table — the table is upgrade-capable tools only.
  </action>
  <verify>
    <automated>grep -q "── Windows Packages (WSL2 only — audit only) ──" upkeep/skills/update/SKILL.md &amp;&amp; grep -q "winget       N installed" upkeep/skills/update/SKILL.md &amp;&amp; grep -q "On WSL2, the Step 2 \"Windows package managers\"" upkeep/skills/update/SKILL.md &amp;&amp; grep -q "── Windows Packages (WSL2 only) ──" upkeep/skills/update/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "── Windows Packages (WSL2 only — audit only) ──" upkeep/skills/update/SKILL.md` returns exactly `1` (Step 3 heading)
    - `grep -c "── Windows Packages (WSL2 only) ──" upkeep/skills/update/SKILL.md` returns exactly `1` (Step 6 heading)
    - `grep -c "winget       N installed  (upgrade via Windows PowerShell)" upkeep/skills/update/SKILL.md` returns exactly `1` (Step 3 row)
    - `grep -c "winget   ⓘ audit only" upkeep/skills/update/SKILL.md` returns exactly `1` (Step 6 row)
    - `grep -c "On WSL2, the Step 2" upkeep/skills/update/SKILL.md` returns exactly `1` (Step 5 audit-only note)
    - No upgrade-capable row for winget/scoop/choco in the Step 5 table: `grep -E "^\| (winget|scoop|choco) \|" upkeep/skills/update/SKILL.md` returns NO matches (zero)
    - Ordering: Step 3 Windows row group appears after `Codex skills` and before the `## Step 4` header
    - Ordering: Step 6 Windows row group appears after `Codex skills   12` and before the `## Rules` header
  </acceptance_criteria>
  <done>Step 3 Overview Table, Step 5 note, and Step 6 Final Report all surface the Windows package managers as audit-only rows — visible but never upgraded.</done>
</task>

</tasks>

<verification>
After all three tasks:

1. `grep -c "Running in WSL2 on Windows" upkeep/skills/update/SKILL.md` returns `1`
2. `grep -c "/mnt/c" upkeep/skills/update/SKILL.md` returns at least `1` (Step 2 accessibility guard)
3. `grep -cE "Bash\((winget|scoop|choco) \*\)" upkeep/skills/update/SKILL.md` returns `3`
4. `grep -cE "command -v (winget|scoop|choco)" upkeep/skills/update/SKILL.md` returns `3`
5. `grep -E "^\| (winget|scoop|choco) \|" upkeep/skills/update/SKILL.md` returns zero matches (no upgrade-table rows)
6. `grep -c "sudo" upkeep/skills/update/SKILL.md` is `0` (update has never had sudo; this plan does not introduce any)
7. The file still passes as valid markdown: no unterminated code fences. Verify by counting backticks: `grep -c "^\`\`\`" upkeep/skills/update/SKILL.md` returns an EVEN number
</verification>

<success_criteria>
- WSL-01 satisfied for update: banner fires in WSL2 before any step
- WSL-04 satisfied: Windows package managers (winget, scoop, choco) are detected via command -v, reported with installed-package counts where accessible, and surfaced as "audit only" rows in the Step 3 Overview Table and Step 6 Final Report
- NO upgrade commands for winget/scoop/choco anywhere in executable bash fences
- /mnt/c/ accessibility guard is present — WSL2 without Windows drive mounted prints a clean skip line
- Allowed-tools extended with the three new query commands, scoped via command -v guards at runtime
- Step 5 contains an explicit note making the audit-only posture machine-checkable
</success_criteria>

<output>
After completion, create `.planning/phases/03-wsl2-support/03-03-SUMMARY.md` capturing:
- Files modified (update/SKILL.md — with line ranges for each insertion: banner, allowed-tools additions, Step 2 block, Step 3 table row group, Step 5 note, Step 6 row group)
- Verification grep counts
- Confirmation of zero upgrade-table rows for winget/scoop/choco
- Confirmation of even backtick count (no unterminated fences)
</output>
